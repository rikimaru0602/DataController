<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.example.rainbowapps.datacontroller01.ImplistActivity">

    <ListView
        android:id="@+id/contents_list"
        android:layout_width="match_parent"
        android:layout_height="700dip"
        android:clipToPadding="false"
        android:paddingBottom="8dp"
        android:paddingTop="8dp"
        android:choiceMode="multipleChoice"
        android:scrollbarStyle="outsideOverlay" />

    <!-- 新規追加-->
    <Button
        android:id="@+id/button_addnew"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:background="@null"
        android:text="@string/page_AddNew"
        android:textColor="#0000FF" />
    <!-- 確定-->
    <Button
        android:id="@+id/button_confirm_imp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:background="@null"
        android:text="@string/text_confirm"
        android:textColor="#0000FF" />
    <!--トップへ-->
    <Button
        android:id="@+id/button_top"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:background="@null"
        android:text="@string/page_Top"
        android:textColor="#0000FF" />

</RelativeLayout>
